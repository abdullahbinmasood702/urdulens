Trained weights go here as `urdulens_crnn.pt` (gitignored). Train with `urdulens train` (see docs/TRAINING.md)
and publish the file as a GitHub Release asset. Override the location with the `URDULENS_MODEL` environment variable.
