"""UrduLens: an open benchmark and toolkit for Urdu OCR.

Importing the package is cheap on purpose (no heavy dependencies), so the
dashboard can run on a small free host without torch or an OCR engine.
"""

__version__ = "0.1.0"
